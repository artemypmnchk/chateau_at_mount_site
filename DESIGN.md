# Дизайн-система Chateau At Mount

Направление: «напский» редакционный стиль (референсы: BRAND Napa Valley, Realm Cellars,
Epoch). Уникальность важнее «люксовых» шаблонных приёмов.

## Шрифты

- **Заголовки:** Oranienbaum (узкий акцидентный дидон, одно начертание 400).
  Кириллица родная, сильный авторский характер — «дорого», не шаблонно.
  Вес 500 в стилях не синтезируем: `font-synthesis-weight: none` на body → чистый 400.
- **Текст/UI:** Inter.
- **Курсив «голоса семьи»** — Piazzolla italic (`--serif-italic`): тэглайны, акцент
  манифеста, номера вин, строка наград, цитаты отзывов, шаги, гастропары.
  У Oranienbaum курсива нет, поэтому наклон несёт настоящий италик, а не фейковый.
- Никаких новых шрифтов без решения владельца.

## Палитра — «вино и известняк»

| Токен | Значение | Роль |
|---|---|---|
| `--bg` | `#f4f1ea` | известняк — светлая основа |
| `--bg-soft` | `#eae5da` | подложки на светлом |
| `--ink` | `#241a1c` | текст, почти-чёрный со сливовым подтоном |
| `--ink-soft` | `#6a5d5e` | вторичный текст |
| `--wine` | `#5e2a33` | **цвет действия** — заливка кнопок на светлом |
| `--accent-dark` | `#6d3440` | винный для киккеров/деталей на светлом |
| `--accent` | `#d0a09b` | пыльная роза — киккеры/детали на тёмном; на страницах вин переопределяется цветом этикетки |
| `--dark` | `#1e1518` | тёмные секции, сливовый (не кофейный) |
| `--clay` | `#8d5843` | обожжённая глина — полотно блока вин на главной |

## Железные правила

1. **Никакого цвета поверх фотографий.** На фото — только белый/монохром:
   текстовые ссылки `.hero-link` (жест «линия-штрих» — разрядка + линия со
   стрелкой, вытягивается на ховере), никаких залитых кнопок.
2. **Винная заливка живёт только на светлом фоне.** На тёмных секциях кнопка светлая
   (`.section-dark .btn-accent`). Ховер главной кнопки — «уголёк» (адаптация
   приёма Пачки): по кромке ходит conic-дуга (`ember-spin`; слои `::before`
   с вращающимся градиентом + `::after`-заливка с отступом 1px + текст в `span`).
   Инверсий заливки в белый на ховере больше нет — палитра не размывается.
   Система жестов: залитая кнопка `.btn-accent` (главное действие) · линия-штрих
   `.hero-link` / `.cta-telegram` (переход) · серифный курсив `.tg-link` (ссылка
   в тексте).
3. **Hero** — композиция вниз-влево, гигантский заголовок, скрим линейный монохромный.
4. **Меню нумерованное** (01–05), языки — текстом с подчёркиванием активного.
5. **Награды** — почётный ряд с медалями, не чипы и не бейджи.
6. Радиусы 2–3px, никаких «пилюль» и кругов с иконками; разрядка (letter-spacing)
   только в САПSLOCK-микролейблах.
7. **Блок вин на главной** — три главы по линейкам (Classic / Rare /
   Experimental), без пин-скролла (решение владельца 03.09.2026). Глава =
   имя линейки, ряд вин, строка характера, «Смотреть линейку» → /wines#линейка;
   чётные главы зеркальны. Ряд (`components/LineRow.tsx`) живёт в двух
   состояниях: покой — **коллаж** (бутылки стянуты к центральной, она спереди
   и крупнее, соседи ярусами назад, подписи погашены); раскрыто — **лента**
   (нативный горизонтальный scroll-snap, бутылка на слайд, имя + строка
   наград с переносом, без счётчика и «Подробнее» — бутылка целиком ссылка,
   соседи гаснут по расстоянию). Раскрытие — клик/тап, горизонтальное
   перетаскивание, стрелки, клавиши ← →, «листать»; уход из вьюпорта
   возвращает коллаж. Высота сцены в обоих состояниях одинакова. Порядок
   вин — `lib/lines.ts`: «первое к читателю» вино в центре (Classic —
   Viorica). Короткий ряд в покое крупнее (`--k`: четыре бутылки +18%, пять
   +12%), чтобы Rare не выглядел мелким рядом с Classic. Заголовок, строка
   характера и «Смотреть линейку» — один компактный блок напротив середины
   сцены. Ховер по коллажу (десктоп) — «дыхание»: соседи расходятся на 6px
   на ярус, центр поднимается на 4px, ореол теплеет на 15%. Движение ряда
   (раскрытие и дыхание) — 0.9s, cubic-bezier(0.25, 0.1, 0.25, 1), мягкий
   старт без рывка. Свечение за рядом — ореол в полтора раза шире группы: цвет
   линейки в покое, `wine.band` центральной бутылки в ленте. Сцена тёмная
   (#171114), в тон манифесту.
8. **Scroll-reveal** — единый язык движения: `[data-reveal]` + хук `useReveal`,
   всплытие из 24px за 0.9s по cubic-bezier(0.215, 0.61, 0.355, 1), каскад через
   `--reveal-delay`; `prefers-reduced-motion` отключает.
